{
  "name": "drive-bunny-stream",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "postinstall": "node scripts/run-prisma-generate.cjs",
    "prebuild": "node scripts/run-prisma-generate.cjs",
    "dev": "concurrently -k -n api,web -c cyan,magenta \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace frontend && npm run build --workspace backend",
    "start": "npm run start --workspace backend",
    "lint": "npm run lint --workspace backend && npm run lint --workspace frontend",
    "prisma:generate": "npm run prisma:generate --workspace backend",
    "prisma:push": "npm run prisma:push --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
